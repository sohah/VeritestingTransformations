node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((not out) => (not (c xor b))) and ((c => (not b)) => out)));

  --%PROPERTY p1;

tel;
