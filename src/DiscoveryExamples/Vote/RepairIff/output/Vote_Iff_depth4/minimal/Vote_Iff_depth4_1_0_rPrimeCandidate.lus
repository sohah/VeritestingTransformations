node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((not (not c)) => ((out or out) xor (a xor b))) and (((c and b) or (b and out)) => ((out and out) xor (out and c)))));

  --%PROPERTY p1;

tel;
