node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((out => c) => (not b)) and ((not b) => ((out => c) xor a))));

  --%PROPERTY p1;

tel;
