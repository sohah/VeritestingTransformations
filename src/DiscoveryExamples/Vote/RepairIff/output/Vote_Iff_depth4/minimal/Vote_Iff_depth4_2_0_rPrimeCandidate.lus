node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((c and a) or (a => b)) => ((b xor out) xor (c => b))) and (((not c) xor (c => a)) => ((not b) and (b => b)))));

  --%PROPERTY p1;

tel;
