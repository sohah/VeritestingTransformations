node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((c and b) => a) => ((out => b) xor (c or a))) and ((not (c xor a)) => ((out => b) xor (out xor b)))));

  --%PROPERTY p1;

tel;
