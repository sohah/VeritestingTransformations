node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((a and b) => ((a xor out) => (c or a))) and (((a xor b) => (not out)) => ((a or b) xor (out => c)))));

  --%PROPERTY p1;

tel;
