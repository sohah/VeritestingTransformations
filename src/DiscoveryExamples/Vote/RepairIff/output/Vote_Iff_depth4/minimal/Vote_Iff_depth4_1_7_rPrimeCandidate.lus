node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((b xor out) xor a) => ((a or b) xor c)) and ((not (out or c)) => (not (c or b)))));

  --%PROPERTY p1;

tel;
