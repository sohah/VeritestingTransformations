node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((b xor (b or a)) => (c => b)) and ((not (b and out)) => ((not out) xor (a or c)))));

  --%PROPERTY p1;

tel;
