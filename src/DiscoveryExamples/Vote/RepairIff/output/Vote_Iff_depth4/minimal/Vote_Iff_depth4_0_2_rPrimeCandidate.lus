node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((c => a) or (a xor b)) => ((not out) xor (a or b))) and (((c xor b) => (b or out)) => (b => b))));

  --%PROPERTY p1;

tel;
