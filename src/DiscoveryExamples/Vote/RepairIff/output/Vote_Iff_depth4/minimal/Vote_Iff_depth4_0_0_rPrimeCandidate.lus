node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((out xor out) xor (out and a)) => (out => c)) and (((c xor c) or (b and b)) => (a => a))));

  --%PROPERTY p1;

tel;
