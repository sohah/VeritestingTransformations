node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((a xor out) xor b) => (not a)) and (((a or c) xor (out or b)) => ((out and out) and (b or c)))));

  --%PROPERTY p1;

tel;
