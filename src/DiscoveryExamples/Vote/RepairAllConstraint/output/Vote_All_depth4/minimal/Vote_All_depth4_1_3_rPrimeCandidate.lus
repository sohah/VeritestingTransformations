node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((out = c) or b) xor out) => (out = c)));

  --%PROPERTY p1;

tel;
