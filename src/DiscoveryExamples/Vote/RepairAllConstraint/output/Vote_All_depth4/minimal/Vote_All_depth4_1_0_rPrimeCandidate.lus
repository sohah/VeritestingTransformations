node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((not (not (a xor out))) => (((not out) => (b => b)) xor ((out => a) => (c xor out)))));

  --%PROPERTY p1;

tel;
