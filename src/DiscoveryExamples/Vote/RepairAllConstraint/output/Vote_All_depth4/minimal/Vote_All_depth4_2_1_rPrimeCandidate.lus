node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (not (((not b) and (b xor out)) and (not (a and c)))));

  --%PROPERTY p1;

tel;
