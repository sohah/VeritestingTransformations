node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((out xor b) and (a => b)) => (not ((c xor b) xor (c => a)))));

  --%PROPERTY p1;

tel;
