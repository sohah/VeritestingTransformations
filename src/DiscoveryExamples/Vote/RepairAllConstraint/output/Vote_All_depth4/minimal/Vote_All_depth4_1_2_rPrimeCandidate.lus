node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((out or (out or out)) => (((c or c) and a) xor (not (out xor b)))));

  --%PROPERTY p1;

tel;
