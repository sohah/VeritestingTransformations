node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((c xor (a => c)) xor ((not b) or (out or a))));

  --%PROPERTY p1;

tel;
