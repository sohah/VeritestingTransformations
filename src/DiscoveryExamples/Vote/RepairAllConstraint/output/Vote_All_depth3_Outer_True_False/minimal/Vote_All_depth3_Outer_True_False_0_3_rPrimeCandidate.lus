node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((not b) xor (c xor a)) or ((c => c) xor out)));

  --%PROPERTY p1;

tel;
