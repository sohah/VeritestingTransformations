node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((c and c) xor (not out)) or ((not out) xor (b or b))));

  --%PROPERTY p1;

tel;
