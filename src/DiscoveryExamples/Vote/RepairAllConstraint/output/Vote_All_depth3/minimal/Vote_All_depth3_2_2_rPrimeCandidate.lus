node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((b xor out) and (a => out)) => ((c xor false) and (b or a))));

  --%PROPERTY p1;

tel;
