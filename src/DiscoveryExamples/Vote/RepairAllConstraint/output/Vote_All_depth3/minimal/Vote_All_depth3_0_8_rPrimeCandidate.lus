node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((b or out) or (c and a)) => (not (not b))));

  --%PROPERTY p1;

tel;
