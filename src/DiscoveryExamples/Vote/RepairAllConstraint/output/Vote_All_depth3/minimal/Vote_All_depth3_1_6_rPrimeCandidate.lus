node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((out and out) xor (b => a)) or ((c and a) xor b)));

  --%PROPERTY p1;

tel;
