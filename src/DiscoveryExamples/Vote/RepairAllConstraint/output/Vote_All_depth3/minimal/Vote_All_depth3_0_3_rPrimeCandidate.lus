node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((false and b) or ((b or c) or (a xor a))));

  --%PROPERTY p1;

tel;
