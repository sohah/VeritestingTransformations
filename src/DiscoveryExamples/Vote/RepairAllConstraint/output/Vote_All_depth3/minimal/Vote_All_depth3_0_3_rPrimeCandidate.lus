node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((b and b) => ((a and b) => (out or out))));

  --%PROPERTY p1;

tel;
