node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((b => a) xor (b => c)) or ((not out) or (c and a))));

  --%PROPERTY p1;

tel;
