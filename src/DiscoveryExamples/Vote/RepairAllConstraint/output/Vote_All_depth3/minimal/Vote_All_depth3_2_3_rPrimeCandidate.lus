node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((c xor b) and (a xor false)) or ((out => b) and (out => c))));

  --%PROPERTY p1;

tel;
