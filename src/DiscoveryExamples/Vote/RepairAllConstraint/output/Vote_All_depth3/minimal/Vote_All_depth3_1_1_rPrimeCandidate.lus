node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((true xor a) or (c => b)) or ((a => out) and (true and out))));

  --%PROPERTY p1;

tel;
