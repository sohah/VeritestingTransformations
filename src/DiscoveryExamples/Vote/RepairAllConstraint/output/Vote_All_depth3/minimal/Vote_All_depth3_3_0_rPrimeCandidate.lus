node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((a and c) xor b) or ((true => out) => (b or false))));

  --%PROPERTY p1;

tel;
