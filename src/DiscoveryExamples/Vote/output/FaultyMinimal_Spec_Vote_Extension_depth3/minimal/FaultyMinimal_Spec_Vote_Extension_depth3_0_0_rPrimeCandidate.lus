node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (out => (((b and c) or (b and a)) and ((c xor a) xor (not b)))));

  --%PROPERTY p1;

tel;
