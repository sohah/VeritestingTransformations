node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((not ((a and c) => (b xor c))) => out));

  --%PROPERTY p1;

tel;
