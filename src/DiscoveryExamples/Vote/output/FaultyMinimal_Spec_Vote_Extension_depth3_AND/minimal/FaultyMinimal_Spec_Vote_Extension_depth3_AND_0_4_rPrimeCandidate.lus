node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((((b or c) xor c) xor (b and (true => a))) => out));

  --%PROPERTY p1;

tel;
