node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((not b) and ((c xor false) and (not a))) => out));

  --%PROPERTY p1;

tel;
