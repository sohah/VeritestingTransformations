node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (((not (true => c)) and ((c => true) => (a => c))) => out));

  --%PROPERTY p1;

tel;
