node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> ((not ((true xor a) or (b or c))) => out));

  --%PROPERTY p1;

tel;
