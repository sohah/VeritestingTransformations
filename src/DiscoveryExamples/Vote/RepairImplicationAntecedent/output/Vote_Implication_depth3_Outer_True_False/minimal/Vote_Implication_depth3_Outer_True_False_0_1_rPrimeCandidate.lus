node main(
  a : bool;
  b : bool;
  c : bool;
  threshold : int;
  out : bool
) returns (
  p1 : bool
);
let
  --%MAIN;
  p1 = (true -> (out => (((b xor c) or (c and a)) xor (a => (b xor b)))));

  --%PROPERTY p1;

tel;
