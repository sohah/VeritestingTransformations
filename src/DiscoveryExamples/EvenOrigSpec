

node main(step: bool; out: int) returns (ok: bool);

var
	count : int;
	p1: bool;

let
	count = if(step) then
	               ( 0 -> (1 + (pre count)))
	        else (pre count);

    p1 = ((count mod 2) = 0) => ( 0 < out ) and (out < 10);

  --%PROPERTY p1;

  ok = true -> p1;

tel;
